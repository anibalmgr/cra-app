import { useQuery } from "react-query";
import { fetchLogin } from "./authAPI";

// export async function useLogin() {
//   const { data, status } = useQuery("login", fetchLogin);
//   console.log(data, status);
// }
